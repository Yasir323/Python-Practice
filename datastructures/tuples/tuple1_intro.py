num = 1     # int
print(type(num))
nums = 1,   # tuple
print(type(nums))
nums1 = 1, 2
print(type(nums1))      # tuple
empty_tuple = ()
ls = [1, 2, 3, 4, "hi"]
tup = tuple(ls)
print(tup)
# Tuples are immutable, so we cannot change or assign it's elements
